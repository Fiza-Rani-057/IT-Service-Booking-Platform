=========First Block===========

1. querySelectorAll()

 Function: Multiple elements select karta hai.
 Example:const buttons = document.querySelectorAll("button");

 2. forEach()

 Function: Selected items par one-by-one kaam karta hai.
 Example:buttons.forEach(function(button) {
    button.style.display = "none";
});

 3. window.scrollTo
 Function: Page ko kisi specific position par le jata hai.

 window.scrollTo({
    top: 0,
    behavior: "smooth"
});

==========Second Block ===========
 1. setTimeout()

Function: Code ko thori der baad chalata hai.

Example:
setTimeout(function () {
    alert("Hello");
}, 100);

 Is code ka flow:
goHomeTo("services")
↓
Home show
↓
100ms wait
↓
Services section find
↓
Services tak smooth scroll

setTimeout()
→ code ko kuch time baad chalata hai.

scrollIntoView()
→ kisi specific element tak scroll karta hai.

  ==============Third Block============

  1. confirm()

Function: User se Yes/No confirmation leta hai.
 Example: const result = confirm("Delete account?");

2. return

Function: Function ko wahin rok deta hai.
  Example : if (!confirmDelete) {
    return;
}
 3.DOMContentLoaded
document.addEventListener("DOMContentLoaded", function () {
    showSection("login");
});

Function: Jab HTML page poora load ho jaye, tab andar wala function chalata hai.

 4. localStorage.removeItem()

 Function: localStorage se specific saved data delete karta hai.
 Example:localStorage.removeItem("techserveRole");

 Third Block ka Flow:::
 Delete Account
↓
confirm()
↓
Cancel? → return → stop
Yes?
↓
User email nikalo
↓
Users array lao
↓
filter() se current user remove
↓
Updated array localStorage mein save
↓
removeItem() se login data delete
↓
Signup page

========3rd Block Imp Points========

confirm()
→ user se confirmation leta hai.

return
→ function ko wahi stop kar deta hai.

removeItem()
→ localStorage se saved item delete karta hai.

DOMContentLoaded
 → Jab HTML page poora load ho jaye, tab andar wala function chalata hai.

======Forth Block======
1. .value

 Function:Input ke andar jo user ne likha hai, woh nikalta hai.
 Example:const emailVal = email.value;
 2. .trim()

 Function:Start/end ki extra spaces hata deta hai.
 Example:email.value.trim()

 3. .focus()

Function:Cursor ko us input par le jata hai.
Example:email.focus();

4. localStorage.setItem()

 Function:LocalStorage mein data save karta hai.
Example:
localStorage.setItem("techserveRole", "admin");
key   = techserveRole
value = admin

 5.loginForm.reset()

 Function:Form ki saari entered values clear kar deta hai.
 Example:loginForm.reset();

 6.foundUser = null

 Function:Pehle assume karte hain ke koi user nahi mila.

 Example:let foundUser = null;
 Baad mein matching user mile:
  foundUser = user;

  7.if (foundUser)

 Function:Check karta hai ke user mila ya nahi.

Example: if (foundUser) {
    ...
}
foundUser = null
→ false

foundUser = user object
→ true
 =======4th block ka flow============

Submit
↓
value se email/password lo
↓
trim() se extra spaces hatao
↓
empty hain? → alert + focus()
↓
Admin check
↓
Provider check
↓
users array se matching user dhoondo
↓
foundUser mila?
↓
localStorage mein user ki info save
↓
form reset
↓
Home show

 Code k Imp Points::
.value → input ki value leta hai
.trim() → extra spaces remove
.focus() → cursor input par le aata hai
.setItem() → localStorage mein save
.reset() → form clear
foundUser → matching user store
if(foundUser) → check user mila ya nahi

 ============5th Block===========
1. classList.remove()

 Function:class Remove krna 
 Example:this.classList.remove("fa-eye");
→ fa-eye class hata di.

2. classList.add()

 Function:class addkrna 
 Example:this.classList.add("fa-eye-slash");
→ fa-eye-slash class add ho gayi.

Flow:
Eye click
↓
fa-eye remove
↓
fa-eye-slash add

3. password.type

 Function:Input ka type change/check karta hai.
 Example:password.type = "text";
→ password visible
password.type = "password";
→ password hidden

4. .length

 Function:String ke andar kitne characters hain, woh batata hai.
 Example:signupPassword.length

"abc123"
length → 6
5. !==

Equal nahi hai check karta hai.
signupPassword !== confirmPassword
→ dono passwords different hain?

6. users.push(newUser)

newUser Object
↓
push()
↓
users Array mein add
Notes mein sirf ye add karo:
classList.remove() → CSS class remove
classList.add() → CSS class add
password.type → input type change/check
length → characters ki counting
!== → equal nahi hai
